# ---Environment Configs---
defaults:
  - _self_

env_name: Road_Env  # Used for logging purposes and selection of the corresponding wrapper.
scenario:
  name: road_env
  map_name: ToyExample-v2 # ToyExample-v2 | Cologne-v1 | CologneBonnDusseldorf-v1

# Defines the metric that will be used to evaluate the performance of the agent.
# This metric is returned at the end of an experiment and can be used for hyperparameter tuning.
eval_metric: episode_return

# Whether the environment observations encode implicit agent IDs. If True, the AgentID wrapper is not used.
# This should not be changed.
implicit_agent_id: True
# Whether or not to log the winrate of this environment. This should not be changed as not all
# environments have a winrate metric.
log_win_rate: False

kwargs:
  encoding_type: binary # alternative: one-hot | sinusoidal | none
  include_extra_observations:
     segment_lengths: True
     volumes: True
     capacity: False